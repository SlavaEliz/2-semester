#include <iostream>
#include <string>
using namespace std;

int main () {
    int i, n, j, p = 0;
    string str = "no info";
    getline (cin, str);
    
    while (str[n] != '.') {
        j++;
        n++;
    }
    
    for (int i = 0; i < n; i++) {
        if (str[i] == ' ') {
            a = i; 
            p++
        }
        else if (s)
    }
    cout << n << endl;
    cout << str;
    return 0;
}
